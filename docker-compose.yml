version: "3"
services:

   db:
     image: postgres:14-alpine
     restart: always
     environment:
       POSTGRES_PASSWORD: pluma
       POSTGRES_DB: pluma
     ports:
       - 5435:5432
     volumes:
       - ./postgres-data:/bitnami/postgresql/data
